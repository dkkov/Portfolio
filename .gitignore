# local editor / agent config — contains absolute paths from this machine
.claude/

# source drop for the photos; the 17 the site actually uses live in images/
IMG/
IMG_9638.PNG

# OS junk
Thumbs.db
Desktop.ini
.DS_Store
